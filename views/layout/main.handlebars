<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Blog</title>
  {{!-- need to make CSS --}}
  <link rel="stylesheet" type="text/css" href="/css/style.css">
</head>

<body>
    <nav>
        <a href="/">Home</a>
        <a href="/">Dashboard</a>
        {{#if loggedIn}}
        <button id="logout">Logout</button>
        {{else}}
        <a href="/login">Login</a>
        {{/if}}
    </nav>
    <main>
        <header>
            <h1>Welcome to the Blog!</h1>
        </header>
        <div class="body">
            {{{ body }}}
        </div>
    </main>
    {{!-- make if logged in script --}}
</body>