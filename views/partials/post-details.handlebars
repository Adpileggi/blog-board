{{!-- get a single post --}}
<div class="card">
        <div class="card-body">
            <p class="card-title">{{title}}</p>
            <p class="card-text">Created on {{createdAt}}</p>
        </div>
</div>
